export const getEvents = (state) => state.event.events;
export const getFeedbacks = (state) => state.feedback.feedbacks;
export const getForums = (state) => state.forum.forums;
export const getUserInfo = (state) => state.user.userInfo;
export const getCourses = (state) => state.course.courses;

// 더 세밀한 선택자를 필요에 따라 추가할 수 있습니다.
